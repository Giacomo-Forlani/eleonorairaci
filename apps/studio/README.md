# Studio (Sanity)

Sanity Studio for content management.

## Setup

1. Copy env vars: `cp .env.example .env`
2. Install deps: `pnpm install`
3. Run Studio: `pnpm studio:dev`

## Notes

- Singletons: siteSettings, homePage, aboutPage, cvPage.
- Schemas live in `schemaTypes`.
